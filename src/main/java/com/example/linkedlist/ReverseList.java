package com.example.linkedlist;

//Use three pointers (prev, current, and next) to keep track of the nodes as you traverse the list.
// Repeatedly, isolate a node and reverse its next pointer.

import java.util.LinkedList;

public class ReverseList {
//    public static LinkedList reverseList() {
//
//    }
}
